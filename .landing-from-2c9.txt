{"method":"Runtime.evaluate","params":{"expression":"(() => {\n  const styles = [...document.querySelectorAll('style')].map(s => s.textContent).filter(t => t && t.includes('cursor: none'));\n  const pin = document.querySelector('.browse-pin > a');\n  const body = document.body;\n  const preview = document.querySelector('.browse-preview');\n  const cs = (el) => el ? getComputedStyle(el).cursor : null;\n  const hover = document.querySelector('.browse-chrome');\n  const t = hover ? getComputedStyle(hover).transition : null;\n  return {\n    leakingStyles: styles,\n    pinCursor: cs(pin),\n    bodyCursor: cs(body),\n    previewCursor: cs(preview),\n    chromeTransition: t,\n    pinCount: document.querySelectorAll('.browse-pin').length,\n  };\n})()","returnByValue":true}