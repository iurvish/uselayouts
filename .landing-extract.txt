Implement the new landing page for the uselayouts project and remove the old landing page code.

## Workspace
`/Users/krishna/Personal/uselayouts`

## User request
The user said: "this is our new landing page so implment this one and remvoe the old code" and pasted a large code snippet starting with:

```
const styles = `
  @import url("https://fonts.googleapis.com/css2?family...
```

The full pasted landing page code is in the conversation transcript. You MUST recover the complete code from the transcript before implementing.

Transcript path:
`/Users/krishna/.cursor/projects/Users-krishna-Personal-uselayouts/agent-transcripts/70ea68e4-0dc5-44f3-84bb-788ea96d02f0/70ea68e4-0dc5-44f3-84bb-788ea96d02f0.jsonl`

Search the transcript for keywords like "new landing page", "const styles", "fonts.googleapis", "app/page.tsx", or similar to find the user's full pasted component/styles. Read enough of that message to get the COMPLETE code (it may be very long).

## What to do
1. Find and read the current landing page at `app/page.tsx` (and any related landing components/styles).
2. Recover the full new landing page code from the transcript.
3. Implement it properly in the Next.js App Router project:
   - Replace the old landing page with the new design
   - Adapt Figma-exported / inline-style code into clean project patterns where appropriate (components, CSS modules or globals, Next.js Image/Link, fonts via next/font or link in layout if needed)
   - Preserve existing routing, auth, and site chrome only if the new design requires them — otherwise follow the new design as the page
   - Ensure fonts, assets, and styles work
   - Make sure it loads on desktop and mobile
4. Remove obsolete old landing page code/files that are no longer used (don't delete unrelated shared components).
5. If assets are referenced (images/videos), check public/ or Figma URLs and wire them correctly.
6. Verify the page compiles (typecheck or dev build if feasible).

## Constraints
- Follow existing project conventions (Tailwind, component structure, etc.)
- Frontend design: this IS a landing page — brand first, full-bleed hero if that's what the design has, avoid generic AI aesthetic clichés unless the provided design uses them
- Do NOT commit unless asked
- There was parallel admin R2 media upload work in progress — DO NOT disrupt that unless files collide; focus only on the landing page

## Return
Summarize: what files changed, how the new page was integrated, any assets still needed, and any issues.