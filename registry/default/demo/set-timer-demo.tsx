"use client";

import SetTimer from "@/registry/default/example/set-timer";

export default function SetTimerDemo() {
  return (
    <div className="flex h-full w-full min-w-0 items-center justify-center overflow-hidden">
      <SetTimer />
    </div>
  );
}
