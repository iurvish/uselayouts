---
title: Interactive Book
description: A 3D interactive book component that opens on hover or drag.
---

import { Tab, Tabs } from "fumadocs-ui/components/tabs";
import { Step, Steps } from "fumadocs-ui/components/steps";
import InteractiveBook from "@/registry/default/example/interactive-book";
import { SourceCode } from "@/components/source-code";
import { ComponentPreview } from "@/components/component-preview";

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <ComponentPreview>
      <InteractiveBook />
    </ComponentPreview>
  </Tab>
  <Tab value="Code">
    <SourceCode filePath="registry/default/example/interactive-book.tsx" />

  </Tab>
</Tabs>

## Installation

<Tabs items={['CLI', 'Manual']}>
  <Tab value="CLI">
    ```bash
    npx shadcn@latest add "https://uselayouts.com/r/interactive-book.json"
    ```
  </Tab>
  <Tab value="Manual">
    <Steps>
      <Step>
        ### Copy the code

        Copy the code from the **Code** tab above into `components/interactive-book.tsx`.
      </Step>
      <Step>
        ### Update imports

        Update the imports to match your project structure.
      </Step>
    </Steps>

  </Tab>
</Tabs>

## Usage

```tsx
import InteractiveBook from "@/components/interactive-book";

export default function Page() {
  return (
    <div className="h-[500px] w-full">
      <InteractiveBook />
    </div>
  );
}
```
