---
title: Input Switch
description: An input field with an animated placeholder and toggleable icon switch.
---

import { Tab, Tabs } from "fumadocs-ui/components/tabs";
import { Step, Steps } from "fumadocs-ui/components/steps";
import { ComponentPreview } from "@/components/component-preview";
import InputSwitch from "@/registry/default/example/input-switch";
import { SourceCode } from "@/components/source-code";

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <ComponentPreview align="center">
      <InputSwitch />
    </ComponentPreview>
  </Tab>
  <Tab value="Code">
    <SourceCode filePath="registry/default/example/input-switch.tsx" />

  </Tab>
</Tabs>

## Installation

<Tabs items={['Manual']}>
  <Tab value="Manual">
    <Steps>
      <Step>
        ### Install dependencies

        ```bash
        npm install motion lucide-react @hugeicons/react @hugeicons/core-free-icons
        ```
      </Step>
      <Step>
        ### Install UI components

        Ensure you have the `Input` component from Shadcn UI installed.

        ```bash
        npx shadcn@latest add input
        ```
      </Step>
      <Step>
        ### Copy the code

        Copy the code from the **Code** tab above into `components/input-switch.tsx`.
      </Step>
    </Steps>

  </Tab>
</Tabs>

## Usage

```tsx
import InputSwitch from "@/components/input-switch";

export default function Page() {
  return <InputSwitch />;
}
```
