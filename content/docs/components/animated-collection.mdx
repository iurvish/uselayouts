---
title: Animated Collection
description: A component to switch between List, Card, and Pack views with morphing animations.
---

import { Tab, Tabs } from "fumadocs-ui/components/tabs";
import { Step, Steps } from "fumadocs-ui/components/steps";
import AnimatedCollection from "@/registry/default/example/animated-collection";
import { SourceCode } from "@/components/source-code";
import { ComponentPreview } from "@/components/component-preview";

<Tabs items={["Preview", "Code"]}>
  <Tab value="Preview">
    <ComponentPreview full>
      <AnimatedCollection />
    </ComponentPreview>
  </Tab>
  <Tab value="Code">
    <SourceCode filePath="registry/default/example/animated-collection.tsx" />
  </Tab>
</Tabs>

## Installation

<Tabs items={['CLI', 'Manual']}>
  <Tab value="CLI">
    ```bash
    npx shadcn@latest add "https://uselayouts.com/r/layout-switcher.json"
    ```
  </Tab>
  <Tab value="Manual">
    <Steps>
      <Step>
        ### Install dependencies

        ```bash
        npm install motion @hugeicons/react @hugeicons/core-free-icons
        ```
      </Step>
      <Step>
        ### Copy the code

        Copy the code from the **Code** tab above into `components/layout-switcher.tsx`.
      </Step>
      <Step>
        ### Update imports

        Update the imports to match your project structure.
      </Step>
    </Steps>

  </Tab>
</Tabs>

## Usage

```tsx
import LayoutSwitcher from "@/components/layout-switcher";

export default function Page() {
  return <LayoutSwitcher />;
}
```

## Features

- **Morphing Layout**: Seamlessly transitions between List, Card, and Pack views.
- **Shared Element Transitions**: Elements smoothly resize and reposition during layout changes.
- **Pack View**: A unique "stack" or "pack" view simulation.
- **View Toggle**: Integrated toggle switch for different layout modes.
