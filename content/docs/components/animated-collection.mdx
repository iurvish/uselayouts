---
title: Animated Collection
description: A collection of items with smooth layout transitions and animations.
---

import { Tab, Tabs } from "fumadocs-ui/components/tabs";
import { Step, Steps } from "fumadocs-ui/components/steps";
import { SourceCode } from "@/components/source-code";
import { ComponentPreview } from "@/components/component-preview";

<Tabs items={["Preview", "Code"]}>
  <Tab value="Preview">
    <ComponentPreview name="animated-collection" full />
  </Tab>
  <Tab value="Code">
    <SourceCode name="animated-collection" />
  </Tab>
</Tabs>

## Installation

<Tabs items={['CLI', 'Manual']}>
  <Tab value="CLI">
    ```bash
    npx shadcn@latest add "https://uselayouts.com/r/animated-collection.json"
    ```
  </Tab>
  <Tab value="Manual">
    <Steps>
      <Step>
        ### Install dependencies

        ```bash
        npm install motion @hugeicons/core-free-icons @hugeicons/react clsx tailwind-merge
        ```
      </Step>
      <Step>
        ### Copy the code

        Copy the code from the **Code** tab above into `components/animated-collection.tsx`.
      </Step>
      <Step>
        ### Update imports

        Update the import paths to match your project setup.
      </Step>
    </Steps>
  </Tab>
</Tabs>

## Usage

```tsx
import AnimatedCollection from "@/components/animated-collection";

export default function Example() {
  return <AnimatedCollection />;
}
```

## Features

- Morphing Layout: Seamlessly transitions between List, Card, and Pack views.
- Shared Element Transitions: Elements smoothly resize and reposition during layout changes.
- Pack View: A unique "stack" or "pack" view simulation.
- View Toggle: Integrated toggle switch for different layout modes.
