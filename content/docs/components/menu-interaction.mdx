---
title: Menu Interaction
description: A morphing menu interaction with smooth animations.
---

import { Tab, Tabs } from "fumadocs-ui/components/tabs";
import { Step, Steps } from "fumadocs-ui/components/steps";
import MenuInteraction from "@/registry/default/example/menu-interaction";
import { SourceCode } from "@/components/source-code";
import { ComponentPreview } from "@/components/component-preview";

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
  <ComponentPreview align="start">
      <div className="flex items-start relative h-[400px] justify-center w-full pt-6">
        <MenuInteraction />
      </div>
  </ComponentPreview>
  </Tab>
  <Tab value="Code">
    <SourceCode filePath="registry/default/example/menu-interaction.tsx" />

  </Tab>
</Tabs>

## Installation

<Tabs items={['CLI', 'Manual']}>
  <Tab value="CLI">
    ```bash
    npx shadcn@latest add "https://uselayouts.com/r/menu-interaction.json"
    ```
  </Tab>
  <Tab value="Manual">
    <Steps>
      <Step>
        ### Copy the code

        Copy the code from the **Code** tab above into `components/menu-interaction.tsx`.
      </Step>
      <Step>
        ### Update imports

        Update the imports to match your project structure.
      </Step>
    </Steps>

  </Tab>
</Tabs>

## Usage

```tsx
import MenuInteraction from "@/components/menu-interaction";

export default function Page() {
  return (
    <div className="flex items-center justify-center h-[400px] w-full">
      <MenuInteraction />
    </div>
  );
}
```
