---
title: Accessible Action
description: A photo deck you can tilt, swipe, and send to the back — like sorting prints on a table.
---

## Features

- **Swipe to the back**: Drag the front photo aside and it drops behind the pile.
- **3D tilt**: The card leans with your pointer so the stack feels physical, not flat.
- **Click to cycle**: A tap on the front card can send it back without a full swipe.
- **Random rotation**: Each card sits at a slight deterministic angle so the pile looks lived-in.
- **Spring settle**: After the flick, cards snap into depth instead of popping off.

## Usage

Feed it a short stack of images — five is plenty. Needs room to drag, so give it a generous preview area. Works as a tactile gallery, not a lightbox: people browse, they don’t zoom. Wire `onSwipe` if you want to track which print they just tossed.

```tsx
import AccessibleAction from "@/components/accessible-action";

export default function Example() {
  return <AccessibleAction />;
}
```

## Where to use

- Photography portfolios and lookbooks
- Mood-board sections on a studio site
- Product colorway or finish pickers
- Case-study image decks on a landing page
- About-the-team photo stacks
